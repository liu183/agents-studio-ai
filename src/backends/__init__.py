"""Provider backends (Image / Video / Text / TTS) and their registry.

M1 ships mock adapters only. Real adapters land in later milestones; see
packages/adapters/types.ts for the canonical TypeScript interface this Python
package mirrors.
"""
